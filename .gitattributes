# 构建过程会重新生成的文件固定成 LF。
#
# 不固定的话，在 autocrlf=true 的 Windows 检出上会出现"幽灵改动":
# 检出时 git 把它们转成 CRLF，随后 Tauri 的 cargo build 用 LF 原样重写
# （权限 toml 每次构建都写一遍），于是 status 里一排 modified，diff 却是
# 空的 —— 每次构建都弄脏工作区，真改动被淹在里面。
src-tauri/permissions/autogenerated/*.toml text eol=lf
schemas/*.json text eol=lf
src/bridge/generated.ts text eol=lf
# tauri CLI 每次 build 都原样重写它（LF），不固定的话同样每次都"被改动"。
src-tauri/Cargo.toml text eol=lf
